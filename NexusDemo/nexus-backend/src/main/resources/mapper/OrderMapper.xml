<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nexus.mapper.OrderMapper">

    <resultMap id="BaseResultMap" type="com.nexus.model.entity.Order">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="order_sn" property="orderSn" jdbcType="VARCHAR"/>
        <result column="member_id" property="memberId" jdbcType="BIGINT"/>
        <result column="total_amount" property="totalAmount" jdbcType="DECIMAL"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="receiver_name" property="receiverName" jdbcType="VARCHAR"/>
        <result column="receiver_phone" property="receiverPhone" jdbcType="VARCHAR"/>
        <result column="receiver_address" property="receiverAddress" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, order_sn, member_id, total_amount, status, receiver_name, receiver_phone, receiver_address, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.nexus.model.entity.Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO oms_order (order_sn, member_id, total_amount, status, receiver_name, receiver_phone, receiver_address)
        VALUES (#{orderSn}, #{memberId}, #{totalAmount}, #{status}, #{receiverName}, #{receiverPhone}, #{receiverAddress})
    </insert>

    <update id="updateById" parameterType="com.nexus.model.entity.Order">
        UPDATE oms_order
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="receiverName != null">receiver_name = #{receiverName},</if>
            <if test="receiverPhone != null">receiver_phone = #{receiverPhone},</if>
            <if test="receiverAddress != null">receiver_address = #{receiverAddress},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM oms_order WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM oms_order
        WHERE id = #{id}
    </select>

    <select id="selectByOrderSn" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM oms_order
        WHERE order_sn = #{orderSn}
    </select>

    <select id="selectByMemberId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM oms_order
        WHERE member_id = #{memberId}
        ORDER BY create_time DESC
    </select>

    <select id="selectByMemberIdAndStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM oms_order
        WHERE member_id = #{memberId} AND status = #{status}
        ORDER BY create_time DESC
    </select>

    <select id="countByMemberId" parameterType="java.lang.Long" resultType="java.lang.Long">
        SELECT COUNT(*) FROM oms_order WHERE member_id = #{memberId}
    </select>

    <select id="countByMemberIdAndStatus" resultType="java.lang.Long">
        SELECT COUNT(*) FROM oms_order WHERE member_id = #{memberId}
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

</mapper>